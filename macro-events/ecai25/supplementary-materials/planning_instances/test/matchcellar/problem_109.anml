type Match;
type Fuse;
fluent boolean handfree;
fluent boolean light;
fluent boolean match_used(Match match);
fluent boolean fuse_mended(Fuse fuse);
constant float [0.0, 6.0] mend_fuse_duration;
fluent integer [0, 1] count_mend_fuse;
action light_match(Match m) {
   duration >= 7 and duration <= 7;
   [ start ] (not match_used(m));
   [ start ] match_used(m) := true;
   [ start ] light := true;
   [ end ] light := false;
   [ end ] count_mend_fuse := 0;
};
action mend_fuse(Fuse f) {
   duration >= mend_fuse_duration and duration <= mend_fuse_duration;
   [ start ] handfree;
   [ start, end ] light;
   [ start ] handfree := false;
   [ end ] fuse_mended(f) := true;
   [ end ] handfree := true;
   [ end ] count_mend_fuse :increase 1;
};
instance Match m23, m24, m25, m26, m27, m28, m29, m30, m31, m32, m33, m34, m35, m36;
instance Fuse f23, f24, f25, f26, f27, f28, f29, f30, f31, f32, f33, f34, f35, f36;
[ start ] handfree := true;
[ start ] light := false;
[ start ] match_used(m23) := false;
[ start ] match_used(m24) := false;
[ start ] match_used(m25) := false;
[ start ] match_used(m26) := false;
[ start ] match_used(m27) := false;
[ start ] match_used(m28) := false;
[ start ] match_used(m29) := false;
[ start ] match_used(m30) := false;
[ start ] match_used(m31) := false;
[ start ] match_used(m32) := false;
[ start ] match_used(m33) := false;
[ start ] match_used(m34) := false;
[ start ] match_used(m35) := false;
[ start ] match_used(m36) := false;
[ start ] fuse_mended(f23) := false;
[ start ] fuse_mended(f24) := false;
[ start ] fuse_mended(f25) := false;
[ start ] fuse_mended(f26) := false;
[ start ] fuse_mended(f27) := false;
[ start ] fuse_mended(f28) := false;
[ start ] fuse_mended(f29) := false;
[ start ] fuse_mended(f30) := false;
[ start ] fuse_mended(f31) := false;
[ start ] fuse_mended(f32) := false;
[ start ] fuse_mended(f33) := false;
[ start ] fuse_mended(f34) := false;
[ start ] fuse_mended(f35) := false;
[ start ] fuse_mended(f36) := false;
mend_fuse_duration := (6/1);
[ start ] count_mend_fuse := 0;
[ end ] fuse_mended(f23);
[ end ] fuse_mended(f24);
[ end ] fuse_mended(f25);
[ end ] fuse_mended(f26);
[ end ] fuse_mended(f27);
[ end ] fuse_mended(f28);
[ end ] fuse_mended(f29);
[ end ] fuse_mended(f30);
[ end ] fuse_mended(f31);
[ end ] fuse_mended(f32);
[ end ] fuse_mended(f33);
[ end ] fuse_mended(f34);
[ end ] fuse_mended(f35);
[ end ] fuse_mended(f36);
