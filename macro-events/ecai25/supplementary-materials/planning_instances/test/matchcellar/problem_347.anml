type Match;
type Fuse;
fluent boolean handfree;
fluent boolean light;
fluent boolean match_used(Match match);
fluent boolean fuse_mended(Fuse fuse);
constant float [0.0, 6.0] mend_fuse_duration;
fluent integer [0, 1] count_mend_fuse;
action light_match(Match m) {
   duration >= 7 and duration <= 7;
   [ start ] (not match_used(m));
   [ start ] match_used(m) := true;
   [ start ] light := true;
   [ end ] light := false;
   [ end ] count_mend_fuse := 0;
};
action mend_fuse(Fuse f) {
   duration >= mend_fuse_duration and duration <= mend_fuse_duration;
   [ start ] handfree;
   [ start, end ] light;
   [ start ] handfree := false;
   [ end ] fuse_mended(f) := true;
   [ end ] handfree := true;
   [ end ] count_mend_fuse :increase 1;
};
instance Match m3, m4, m5, m6, m7, m8, m9, m10, m11, m12, m13, m14, m15, m16, m17, m18, m19, m20, m21, m22, m23, m24, m25, m26, m27, m28;
instance Fuse f3, f4, f5, f6, f7, f8, f9, f10, f11, f12, f13, f14, f15, f16, f17, f18, f19, f20, f21, f22, f23, f24, f25, f26, f27, f28;
[ start ] handfree := true;
[ start ] light := false;
[ start ] match_used(m3) := false;
[ start ] match_used(m4) := false;
[ start ] match_used(m5) := false;
[ start ] match_used(m6) := false;
[ start ] match_used(m7) := false;
[ start ] match_used(m8) := false;
[ start ] match_used(m9) := false;
[ start ] match_used(m10) := false;
[ start ] match_used(m11) := false;
[ start ] match_used(m12) := false;
[ start ] match_used(m13) := false;
[ start ] match_used(m14) := false;
[ start ] match_used(m15) := false;
[ start ] match_used(m16) := false;
[ start ] match_used(m17) := false;
[ start ] match_used(m18) := false;
[ start ] match_used(m19) := false;
[ start ] match_used(m20) := false;
[ start ] match_used(m21) := false;
[ start ] match_used(m22) := false;
[ start ] match_used(m23) := false;
[ start ] match_used(m24) := false;
[ start ] match_used(m25) := false;
[ start ] match_used(m26) := false;
[ start ] match_used(m27) := false;
[ start ] match_used(m28) := false;
[ start ] fuse_mended(f3) := false;
[ start ] fuse_mended(f4) := false;
[ start ] fuse_mended(f5) := false;
[ start ] fuse_mended(f6) := false;
[ start ] fuse_mended(f7) := false;
[ start ] fuse_mended(f8) := false;
[ start ] fuse_mended(f9) := false;
[ start ] fuse_mended(f10) := false;
[ start ] fuse_mended(f11) := false;
[ start ] fuse_mended(f12) := false;
[ start ] fuse_mended(f13) := false;
[ start ] fuse_mended(f14) := false;
[ start ] fuse_mended(f15) := false;
[ start ] fuse_mended(f16) := false;
[ start ] fuse_mended(f17) := false;
[ start ] fuse_mended(f18) := false;
[ start ] fuse_mended(f19) := false;
[ start ] fuse_mended(f20) := false;
[ start ] fuse_mended(f21) := false;
[ start ] fuse_mended(f22) := false;
[ start ] fuse_mended(f23) := false;
[ start ] fuse_mended(f24) := false;
[ start ] fuse_mended(f25) := false;
[ start ] fuse_mended(f26) := false;
[ start ] fuse_mended(f27) := false;
[ start ] fuse_mended(f28) := false;
mend_fuse_duration := (6/1);
[ start ] count_mend_fuse := 0;
[ end ] fuse_mended(f3);
[ end ] fuse_mended(f4);
[ end ] fuse_mended(f5);
[ end ] fuse_mended(f6);
[ end ] fuse_mended(f7);
[ end ] fuse_mended(f8);
[ end ] fuse_mended(f9);
[ end ] fuse_mended(f10);
[ end ] fuse_mended(f11);
[ end ] fuse_mended(f12);
[ end ] fuse_mended(f13);
[ end ] fuse_mended(f14);
[ end ] fuse_mended(f15);
[ end ] fuse_mended(f16);
[ end ] fuse_mended(f17);
[ end ] fuse_mended(f18);
[ end ] fuse_mended(f19);
[ end ] fuse_mended(f20);
[ end ] fuse_mended(f21);
[ end ] fuse_mended(f22);
[ end ] fuse_mended(f23);
[ end ] fuse_mended(f24);
[ end ] fuse_mended(f25);
[ end ] fuse_mended(f26);
[ end ] fuse_mended(f27);
[ end ] fuse_mended(f28);
